---
# === Package Management ===
stunnel::packages:
  - stunnel4

# === Core Binaries & Service Identity ===
stunnel::bin_name: stunnel4

# === Paths & Directories ===
stunnel::log_dir: /var/log/stunnel4
